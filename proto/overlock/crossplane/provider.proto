syntax = "proto3";
package overlock.crossplane;

option go_package = "overlock/x/crossplane/types";

// Provider
message Provider {
  // name
  string name = 1;
  // url
  string url = 2;
  // environment
  uint64 environment = 3;
  // id
  uint64 id = 4;
}
